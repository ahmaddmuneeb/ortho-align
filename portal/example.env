# API base URL (no trailing slash). Defaults to https://api.orthoalignsolution.com if unset.
VITE_API_BASE_URL=https://api.orthoalignsolution.com

# Vercel: add the same variable under Project → Settings → Environment Variables
# (Production). Root Directory in Vercel must be set to "portal" for this monorepo.
